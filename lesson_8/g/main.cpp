#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

int main(int argc, char const *argv[])
{
    int n, p;
    vector<int> nums;
    for (int i = 0; i < n; i++)
    {
        int num;
        std::cin >> num;
        nums.push_back(num);
    }
    // for (auto num : nums)
    // {
    //     auto text = "NO";
    //     for (int i = 0; i < p; i++)
    //     {
    //         if (pow(p, i)%m)
    //         {
    //             /* code */
    //         }
            
    //     }
        
    //     std::cout << text << std::endl;
    // }

    return 0;
}
