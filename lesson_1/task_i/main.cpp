#include <string>
#include <iostream>
using namespace std;

char func(string input)
{
}

int main(int argc, char const *argv[])
{
    int length;
    std::cin >> length;
    string input;
    std::cin >> input;
    std::cout << func(input) << endl;
    return 0;
}
